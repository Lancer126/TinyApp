<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>Show URLs</title>
</head>
<body style="font-size:20px; padding:50px;">
    <header><% include ./partials/_header %></header>
    <h1>ShortURL: <%= shortURL %></h1>
    
    <form class="form-inline" action="/urls/<%= shortURL %>" method="POST">
          <label for="urls">Enter a new URL: </label>
          <input class="form-control" type="text" name="urls" placeholder="<%= urls[shortURL].longURL %>" style="width: 300px; margin: 1em">
          <button type="submit" class="btn btn-primary">Update</button>
</body>
</html>